//! FrameDebugger plugin: frame-by-frame debugging

pub struct FrameDebuggerPlugin;

impl FrameDebuggerPlugin {
    pub fn register() {
        // Register with PluginManager
    }
    pub fn init() {
        // Initialize frame debugger
    }
    // --- Feature stubs ---
    pub fn capture_frame(&self) {
        // Capture a frame
    }
    pub fn inspect_frame(&self) {
        // Inspect captured frame
    }
}
